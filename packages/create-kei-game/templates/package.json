{
  "name": "__PROJECT_SLUG__",
  "version": "0.1.0",
  "private": true,
  "description": "__PROJECT_TITLE__ — a browser game with a currency players own.",
  "type": "module",
  "scripts": {
    "dev": "bun run server/main.ts",
    "build": "bun build src/main.ts --outdir public/build --target browser --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@babylonjs/core": "^9.19.0",
    "kei-transaction": "__SDK_VERSION__"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.0"
  }
}
