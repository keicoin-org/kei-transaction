node_modules/
public/build/
dist/
*.tsbuildinfo
.DS_Store

# Which payments the game has already answered (server/orders.ts). It belongs to
# one deployment and one chain, so it is not shared and not committed — but on a
# real node it is the one file worth keeping across a restart.
.kei/

# The issuer's seed is the game's money. It never belongs in a commit.
.env
.env.*
!.env.example
